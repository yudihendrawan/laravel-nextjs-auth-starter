/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "unsplash.com",
      "via.placeholder.com",
      "plus.unsplash.com",
    ],
  },
};

export default nextConfig;
